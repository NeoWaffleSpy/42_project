import { UserNotFoundFilterFilter } from './user-not-found.filter';

describe('UserNotFoundFilterFilter', () => {
  it('should be defined', () => {
    expect(new UserNotFoundFilterFilter()).toBeDefined();
  });
});
